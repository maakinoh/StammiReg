namespace TelegramBot.StammiReg.Bot;

public enum Commands
{
    Idle,
    StartReg,

    // Enters whenever the user want's to attend just the fursuit walk, the furmeet or both
    EnterScope,
    Confirm
}